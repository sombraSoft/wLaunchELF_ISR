name: Automated-Build

on:
  workflow_dispatch:
  push:
  pull_request:
  repository_dispatch:
    types: [run_build]

jobs:
  build:
    runs-on: ubuntu-latest
    container: ps2dev/ps2dev:v1.0
    steps:
    - name: Install dependencies
      run: |
        apk add build-base git zip
    - uses: actions/checkout@v2
    - run: |
        git config --global --add safe.directory "$GITHUB_WORKSPACE"
        git fetch --prune --unshallow
        
    - name: Install ps2eth
      run: |
        git clone https://github.com/ps2dev/ps2eth.git
        cd ps2eth && make clean all install
        
    - name: Compile wLaunchELF without network
      run: |
        make rebuild TMANIP=1
        mv BOOT.ELF BOOT-NO_NETWORK.ELF
        mv BOOT-UNC.ELF BOOT-UNC-NO_NETWORK.ELF
                
    - name: Compile wLaunchELF with EXFAT
      run: |
        make rebuild TMANIP=1 EXFAT=1
        mv BOOT.ELF BOOT-NO_NETWORK-EXFAT.ELF
        mv BOOT-UNC.ELF BOOT-UNC-NO_NETWORK-EXFAT.ELF
        make rebuild TMANIP=1 EXFAT=1 ETH=1
        mv BOOT.ELF BOOT-EXFAT.ELF
        mv BOOT-UNC.ELF BOOT-UNC-EXFAT.ELF
        
    - name: Clean Project
      run: |
        make clean
        
    - name: "Compile wLaunchELF - NO IOP-RESET"
      run: |
        make rebuild TMANIP=1 ETH=0 IOP_RESET=0
        mv BOOT.ELF BOOT-NO_IOP_RESET.ELF
        
    - name: Clean Project
      run: |
        make clean
        
    - name: Compile wLaunchELF with EE-SIO
      run: |
        make rebuild TMANIP=1 ETH=1 SIO_DEBUG=1
        mv BOOT.ELF BOOT-EE_SIO.ELF
        make rebuild clean all TMANIP=1 ETH=0 SIO_DEBUG=1
        mv BOOT.ELF BOOT-NO_NETWORK-EE_SIO.ELF
        
    - name: Clean Project
      run: |
        make clean

    - name: Compile wLaunchELF with DVRP support
      run: |
        make clean all TMANIP=1 ETH=1 DVRP=1
        mv BOOT.ELF BOOT-DVRP.ELF
        make clean all TMANIP=1 ETH=0 DVRP=1
        mv BOOT.ELF BOOT-DVRP-NO_NETWORK.ELF

    - name: Clean Project
      run: |
        make clean

    - name: Compile wLaunchELF
      run: |
        make rebuild TMANIP=1 ETH=1
        
    - name: Get short SHA
      id: slug
      run: echo "::set-output name=sha8::$(echo ${GITHUB_SHA} | cut -c1-8)"

    - name: Upload artifacts
      if: ${{ success() }}
      uses: actions/upload-artifact@v2
      with:
        name: wLaunchELF-4.43x_isr-${{ steps.slug.outputs.sha8 }}
        path: |
              *.ELF
              
    - name: Create release
      if: github.ref == 'refs/heads/master'
      uses: marvinpinto/action-automatic-releases@latest
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "latest"
        title: "Latest automated build"
        files: |
               *.ELF
               
####################################################### 
  build-spanish:
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build
    container: ps2dev/ps2dev:v1.0
    steps:
    - name: Install dependencies
      run: |
        apk add build-base git zip
    - uses: actions/checkout@v2
    - run: |
        git config --global --add safe.directory "$GITHUB_WORKSPACE"
        git fetch --prune --unshallow
        
    - name: Install ps2eth
      run: |
        git clone https://github.com/ps2dev/ps2eth.git
        cd ps2eth && make clean all install

    - name: Compile wLaunchELF
      run: |
        SCH=${GITHUB_SHA::7}
        echo "#include \"Lang/SPA.LNG\"">lang.h
        make TMANIP=1 ETH=1
        mv BOOT.ELF BOOT-SPA.ELF
        mv BOOT-UNC.ELF BOOT-UNC-SPA.ELF

    - name: Compile wLaunchELF without network modules
      run: |
        make clean
        make TMANIP=1 ETH=0
        mv BOOT.ELF BOOT-SPA_NO-NETWORK.ELF
        mv BOOT-UNC.ELF BOOT-UNC-SPA_NO-NETWORK.ELF

    - name: Compile wLaunchELF with DVRP support
      run: |
        make clean all TMANIP=1 ETH=1 DVRP=1
        mv BOOT.ELF BOOT-DVRP.ELF
        make clean all TMANIP=1 ETH=0 DVRP=1
        mv BOOT.ELF BOOT-DVRP-NO_NETWORK.ELF

    - name: Clean Project
      run: |
        make clean

    - name: Get short SHA
      id: slug
      run: echo "::set-output name=sha8::$(echo ${GITHUB_SHA} | cut -c1-8)"

    - name: Upload artifacts
      if: ${{ success() }}
      uses: actions/upload-artifact@v2
      with:
        name: wLaunchELF-spanish-${{ steps.slug.outputs.sha8 }}
        path: |
              *.ELF

    - name: Create release
      if: github.ref == 'refs/heads/master'
      uses: marvinpinto/action-automatic-releases@latest
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "español"
        title: "Ultima compilacion automatica"
        files: |
               *.ELF
               
  trigger-auto-update:
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [build]
    strategy:
      matrix:
        repos: [israpps/FreeMcBoot-Installer, israpps/Funtuna-Fork, israpps/DiagBox]
    steps:
    - name: trigger israpps auto-update system
      if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
      uses: peter-evans/repository-dispatch@v1
      with:
          token: ${{ secrets.IUS }}
          repository: ${{ matrix.repos }} 
          event-type: Component_update
          client-payload: '{"sha": "${{ github.sha }}"}'
